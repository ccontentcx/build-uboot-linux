

<div style="display: flex;">
  <div style="flex: 1; padding-right: 10px;">
    <!-- 左側內容 -->
    <pre>
      直接執行 make menuconfig 修改你的設定，然後直接 make -j$(nproc) V=s 開始編譯。只要你不動到架構（Target），系統會聰明地只編譯你修改的部分
    </pre>
  </div>
  <div style="flex: 1; padding-left: 10px;">
    <!-- 右側內容 -->
    <pre>
      直接執行 make menuconfig 修改你的設定，然後直接 make -j$(nproc) V=s 開始編譯。只要你不動到架構（Target），系統會聰明地只編譯你修改的部分
    </pre>
  </div>
</div>
